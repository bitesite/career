source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in career.gemspec.
gemspec

group :development do
  gem 'pg', '0.21.0'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
ruby '2.5.7'
#ruby-gemset=career
